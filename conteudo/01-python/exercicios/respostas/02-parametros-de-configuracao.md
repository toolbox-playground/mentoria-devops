# Resposta do Exercício 2 - Parametros de Configuração

```python
configuracoes = {'host': 'localhost', 'porta': 8080, 'debug': True}
for chave, valor in configuracoes.items():
    print(f'{chave}: {valor}')
```
